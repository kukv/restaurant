<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.kukv.reservations.infrastructure.datasource.receipt.visit.VisitRecordMapper">
    <insert id="record">
        insert into reservations.visit(
            receipt_number,
            visit_date,
            visit_time,
            number_of_visitors,
            created_at
        )
        values (
            #{receiptNumber.value},
            #{visit.visitDate.value},
            #{visit.visitTime.value},
            #{visit.numberOfVisitors.value},
            #{createdTime.value}
       )
    </insert>
</mapper>
