<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.kukv.reservations.infrastructure.datasource.receipt.holder.profile.ProfileRecordMapper">
    <insert id="record">
        insert into reservations.profile(receipt_number, name, birthdate, created_at)
        values (#{receiptNumber.value}, #{profile.name.name}, #{profile.birthDate.name}, #{createdTime.value})
    </insert>
</mapper>
