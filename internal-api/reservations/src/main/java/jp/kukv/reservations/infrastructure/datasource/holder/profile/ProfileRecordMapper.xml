<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.kukv.reservations.infrastructure.datasource.holder.profile.ProfileRecordMapper">
    <insert id="record">
        insert into reservations.reservation_holder_profile(
            reservation_id,
            name,
            birth_date,
            created_at
        )
        values (
            #{reservationId.value},
            #{profile.name.value},
            #{profile.birthDate.value},
            #{createdTime.value}
        )
    </insert>
</mapper>
